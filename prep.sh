#!/usr/bin/bash

# fix the fucking versions for the cache (do it even on dev branch)
# ..wait i should git actions this maybe?
# copy dev stuff into temp directory into .gitignore zone or different branch
# minify everything with some tool
# git push
# move dev stuff back
